[package]
name = "zero-recovery"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true, features = ["derive"] }
log = { workspace = true }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
mkzfs = { path = "../tools" }
